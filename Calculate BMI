"""
Write a function that asks the user to enter their weight in kilos
and their height in metres, then calculate their BMI.
BMI = Weight / (height x height)
"""
def get_user_info():
    weight = float(input("Enter your weight in kilos > "))  # convert to deciaml
    height = float(input("Enter your height in metres > ")) # convert to deciaml
    return weight, height

def calculate_bmi(weight, height):
    bmi = weight / (height * height)
    return bmi

def main():
    weight, height = get_user_info()
    bmi = calculate_bmi(weight, height)
    print("BMI = ",bmi)

main()
